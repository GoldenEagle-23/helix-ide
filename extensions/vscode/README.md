# VS Code Extension for Helix

This directory contains the source for the VS Code extension that connects to the Helix coding agent. The extension exposes commands such as `Helix: Chat with Agent` and `Helix: Run Agent Task` and requires a running Helix server.
